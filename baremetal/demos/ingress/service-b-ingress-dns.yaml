apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /   #new 
  name: service-b-ingress
  namespace: ingress-demo
  labels:
    external-dns: apps.fabrikam.pl
spec:
  ingressClassName: ingress-demo-controller
  rules:
  - host: demo1.apps.fabrikam.pl
    http:
      paths:
      - path: /b
        pathType: Prefix
        backend:
          service:
            name: service-b
            port:
              number: 80
